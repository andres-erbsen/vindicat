import Data.Serialize
import Data.Word
import Data.ByteString (ByteString)
import qualified Data.ByteString as B

-- VindiCat + Address = Caddress
type Caddress = Integer

-- I do not know what else to write here, so I'll just wait until I get to talk to someone smarter than me.
